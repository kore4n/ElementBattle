using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Fake projectile so that clients don't experience lag firing projectiles
public class FakeProjectile : MonoBehaviour
{
    // TODO: Add logic to make sure visuals sync up with actual projectile (i.e. destroy after seconds, collide with certain layers, color, etc...)
    private void Start()
    {
        
    }
}
